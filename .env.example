# Copy this file to `.env` and add your real key locally.
# `.env` is ignored by git.

# Optional override for an OpenAI-compatible gateway such as an internal CorteX endpoint.
# Example: https://cortex.internal.example/v1
LLM_API_BASE=REPLACE_WITH_GATEWAY_URL

OPENAI_API_KEY=replace-me
GEMINI_API_KEY=replace-me
ANTHROPIC_API_KEY=replace-me
